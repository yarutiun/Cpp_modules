#include "BitcoinExchange.hpp"

int main(int argc, char **argv)
{
    if(argc != 2)
    {
        std::cout << "Wrong input to your programm (should only accept one file)" << std::endl;
        return(0);
    }
}